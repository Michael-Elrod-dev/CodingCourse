using UnityEngine;

// Enemy.cs — Session 4
//
// NEW THIS SESSION: a SECOND file. Until now everything lived in one script
// attached to LessonRunner. This file is different — it is NOT attached to any
// GameObject, and it is NOT a MonoBehaviour. It is a plain class: a blueprint.
//
// A blueprint describes what every Enemy HAS (its data) and what every Enemy
// can DO (its behaviour). The class itself does nothing on its own! Over in
// Session04_Exercise.cs you'll stamp out actual enemies FROM this blueprint
// using the word `new` — and each one gets its own private copy of the data
// described here. One class, many enemies.
//
// You will build this class up across Parts 2, 4, and 5. The runner file tells
// you when to come here and what to add. There's a home marked for each piece.

public class Enemy
{
    // --- FIELDS (Part 2) -------------------------------------------------
    // The data every enemy carries around. Add yours here.



    // --- CONSTRUCTOR (Part 5) --------------------------------------------
    // A special method that sets a fresh enemy up the instant it's created.



    // --- METHODS (Part 4) ------------------------------------------------
    // The things an enemy can DO. Add yours here.


}
